#include <stdio.h>

int main(void)
{
    int num = 0;
    int sum;
    int n = 0;

    while (1)
    {
        scanf("%d", &num);
        sum += num;
        if (num == 0)
        {

            break;
        }
        else if (num == 100)
        {
            break;
        }
        
        
    }
    
}